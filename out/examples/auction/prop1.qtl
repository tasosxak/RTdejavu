
// Bids on items at an auction must strictly incease. That is,
// if a bid a2 is submitted on an item i, and in the past there was another
// bid a1 on that item, then a2 must be bigger than a1.

prop p : Forall i . Forall a1 .  bid(i,a1)  -> a1 >= i + 1